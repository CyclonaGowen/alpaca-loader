# Alpaca Loader

Small Node.js application that connects to the Alpaca Trading API and loads stock market candle data.

## Setup

Install dependencies:

npm install

Create a .env file:

APCA_API_KEY_ID=your_key
APCA_API_SECRET_KEY=your_secret

Run the app:

node src/index.js